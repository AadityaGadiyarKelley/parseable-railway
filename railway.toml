# railway.toml — Parseable service
# Docs: https://docs.railway.com/reference/config-as-code

[build]
builder       = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "parseable local-store"
restartPolicyType  = "ON_FAILURE"
restartPolicyMaxRetries = 10
